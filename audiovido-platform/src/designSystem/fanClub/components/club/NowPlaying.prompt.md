# NowPlaying

Ambient music widget. Sits bottom-right of the chat area. Heart toggles a vote for the next track. The waveform is decorative — bars equalize regardless of actual playback.

```jsx
<NowPlaying track="Nocturne 12" artist="Submerge" progress={0.4} />
```
