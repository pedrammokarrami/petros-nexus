# IconButton

Circular icon-only action. Pair with `label` so screen readers and tooltips work. The optional `badge` dot is for "you have a new thing".

```jsx
<IconButton label="React" variant="glow">😍</IconButton>
<IconButton label="Mic" variant="primary" size="lg">🎤</IconButton>
```
