# Card

Base surface container. `solid` for sidebars, `glass` for overlays on top of the mood gradient, `stage` for the deep recess that holds the CRT.

```jsx
<Card variant="glass" pad={5} glow>…</Card>
```
