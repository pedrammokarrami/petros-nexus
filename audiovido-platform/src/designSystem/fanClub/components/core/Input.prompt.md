# Input

Text field on a dark surface. The focus state pulls the accent in via border + glow — same accent the room is using, so cyan in music rooms, coral in cinema.

```jsx
<Input value={msg} onChange={setMsg} placeholder="Say something…" icon="✎" />
```
