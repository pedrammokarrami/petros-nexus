# Badge

Marquee-style status pill. Always uppercase, always mono. Pair `live` + `pulse` for "happening right now".

```jsx
<Badge variant="live" pulse>OPEN MIC</Badge>
<Badge variant="accent">MEMBER</Badge>
<Badge variant="outline">12 ONLINE</Badge>
```
