# MoodMeter

The animated club-vibe gauge — needle sweeps between 🌙 melancholic and 💥 euphoric. `value` is 0..1; everything else (color, label, glow) follows from there. Use as a corner widget or inside the stage area.

```jsx
<MoodMeter value={0.78} sampleCount={47} />
```
