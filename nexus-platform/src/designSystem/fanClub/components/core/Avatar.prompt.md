# Avatar

Circular member portrait. Background falls back to a hashed gradient + initials when there's no `src`. `active` adds a pulsing accent ring — use it for spotlit speakers, currently-typing members, or the DJ persona.

```jsx
<Avatar name="Reni Q" active />
<Avatar name="Mara" src="/x.png" size={56} status="live" />
```
